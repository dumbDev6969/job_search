I have two HTML files:

The old files :
The new files : 
What I'd like you to do is apply the UI/layout changes from the new files  to the old files , while preserving all functional elements from the old file .

Specifically, keep the following from the old file:

All script tags and custom JavaScript
The CSRF token
All form actions
The name, id, and class attributes of all input fields
In other words, update the appearance/layout (UI) of the old file using the structure/design from the new file, but ensure that the functionality and backend connections (form handling, scripts, CSRF protection) from the old file remain intact. 